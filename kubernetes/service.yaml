apiVersion: v1
kind: Service

metadata:
  name: stopwatch-service
  namespace: default
  labels:
    app: stopwatch

spec:
  selector:
    app: stopwatch

  type: NodePort

  ports:
    - name: http
      protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30080
